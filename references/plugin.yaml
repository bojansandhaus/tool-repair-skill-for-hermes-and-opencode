name: hermes-tool-repair
version: 1.0.0
description: "Deterministic tool call repair — catches the four common JSON mistakes open models make and fixes them before dispatch, with repair notes so the model self-corrects."
author: Hermione
license: MIT
provides_hooks:
  - transform_llm_output
requires_env: []
